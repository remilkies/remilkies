name: Generate Midnight Orchid Snake

on:
  schedule:
    - cron: "0 0 * * *" 
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3 
      
      # Updated this step name too!
      - name: Generate the Witchy Snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=#312A44&color_dots=#161b22,#D7C5D8,#A6AAC8,#88708E,#312A44
            dist/github-snake-dark.svg?color_snake=#312A44&color_dots=#161b22,#D7C5D8,#A6AAC8,#88708E,#312A44
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to Output Branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: output
          commit_message: "🐍 update midnight orchid snake animation"